---
description: MyChat coding practices — style, UX contracts, and safe changes
globs: "**/*.{js,css,html,py,mjs,mdc}"
alwaysApply: false
---

<!-- Quality: 9/10 — contracts thorough and scannable; slight overlap with modularity rule -->

# MyChat coding practices

## Style

- Prefer small, named functions; factories when a module needs DOM/memory callbacks.
- Keep imports at the top of the file (no inline imports).
- Match existing naming: `createX`, `runChat` / `runImage`, `setStatus`, `persistSession`.
- Escape user/tool HTML (`escapeHtml`); Markdown goes through `renderMarkdown` (DOMPurify + `target=_blank`).
- Use `isAbortError` / `StoppedError` for cancel paths; never swallow abort as a normal failure.
- On every create/edit batch: parent does a local Boy Scout cleanup; a **sub-agent** assigns final `Quality: N/10` headers (see `mychat-boyscout.mdc`).

## Product contracts

- Think (`think: true`) only for models in `THINK_MODELS` (today: `qwen3.5:9b`).
- Auto-scroll the thread only when near bottom (`scrollThreadToBottom`).
- Persist the **active chat** after meaningful turns; on quota, retry without image payloads.
- Multi-chat: New chat / switch / delete live in the left sidebar; block switch while `busy`.
- Sidebar is collapsible (ChatGPT/Claude-style): topbar toggle, in-panel close, mobile backdrop; persist `mychat.sidebarCollapsed`; `[` toggles when not typing.
- Clear **this chat** keeps facts & RAG and keeps the chat slot; say so in UI copy.
- **Delete** on a bubble removes that message and everything after (model history + UI transcript).
- **Regenerate** on assistant bubbles redoes from the previous user message.
- **Export chat** downloads the active thread as Markdown (Settings).
- Tool cards (`web_search`) persist in the thread and restore across reloads.
- External links open in a new tab (`noopener noreferrer`).

## When changing flows

- Update Settings toggles + `config.js` keys together if you add memory/chat options.
- Extend `js/memory/*` behind the orchestrator facade; UI should keep talking to `prepareTurn` / settings, not raw stores.
- After navigation / primary CTA / memory / tool / sidebar UX changes, keep or add coverage under `test/` and run `npm test`.

## ❌ Avoid

- Purple/glow “AI slop” restyles; keep the cream + violet/magenta token set in `css/tokens.css`
- `waitForTimeout`-style brittle waits in any future UI tests — assert roles/text/URL
- Broad drive-by refactors unrelated to the request
